import React, {useRef, useState} from 'react';
import {View, BackHandler, Alert, Linking, Platform} from 'react-native';

export const LoginScreen = ({navigation, route}) => {
  return <View style={{flex: 1, backgroundColor: 'red'}}></View>;
};
